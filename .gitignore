.DS_Store
*.swp
*.swo
.idea/
.vscode/
.claude/
node_modules/
__pycache__/
*.pyc

# Local data dumps (don't commit raw Waze JSONs / generated reports)
*.json
*.xlsx
!waze_routes_v2.xlsx
# clasp manifest — must be tracked so push has a manifest to deploy.
!appsscript.json
# clasp scriptId is per-developer; keep .clasp.json local.
.clasp.json
